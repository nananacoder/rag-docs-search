# Session Handoff — M5/M6 + Cost Teardown

**Session dates**: 2026-07-14 → 2026-07-28
**Supersedes context in**: `2026-07-13-m2-m4-shipped.md`

## Where the project stands

Phase 2 is **built, measured, documented, and torn down to near-zero cost.**
The interview deliverables are complete; the live pgvector system is offline
(Cloud SQL deleted) but regenerable.

Branch `stage2-pgvector`, HEAD around `bd2d9b8`.

## What shipped this session

- **M5 part-split** (`app/ingestion/parts.py`, `--split-astro-parts`): the
  4 canonical `openstax-astronomy-2e-pt{1-4}` book_ids are now produced by
  the pipeline, not an ad-hoc SQL migration (removed the golden-contract
  landmine from learnings/11 §7).
- **M5 ef_search sweep** (`eval/runs/m5-ef-search-sweep.md`): recall
  plateaus at ef=20; keep default 40. Retrieval SQL is ~0.2s of the ~19.6s
  p50 — latency lives in the two LLM calls, not the DB.
- **bbox citation highlight** (`web/.../pdf-pane.ts`): ngx-extended-pdf-viewer
  + normalized-bbox→CSS-% overlay. The visible Phase 1→2 win. Verified.
- **GCS PDF proxy** (`api/app/api/pdf.py`): same-origin, HTTP Range,
  streams the corpus from GCS so no 155MB local/committed file. Verified
  end-to-end (206, correct Content-Range, %PDF magic).
- **RAGAS** (`eval/ragas_eval.py`): ragas-the-library is unusable on
  py3.12 (see learnings/11 §8); reimplemented the 3 metrics as direct
  google-genai judge calls. Validated against real Gemini (faithfulness
  0.667 on a crafted sample). Wired as `run_eval.py --ragas`.
- **8 ADRs** (`docs/adr/`), **A/B report** (`docs/phase2-ab-report.md`),
  **demo guide** (`docs/demo-guide.md`), **study prompts**
  (`docs/study-prompts.md`).

## What did NOT complete (and why)

- **Full 8-question paid RAGAS run**: on teardown day, outbound port 3307
  to Cloud SQL was network-blocked (learnings/11 §9). The judge is
  validated but the aggregate RAGAS cells in the A/B report stay `‹RAGAS›`.
- **Phase 1 RAGAS arm**: not run (would need discovery_engine + more spend).

## Cloud footprint now (near-zero)

- **Cloud SQL rag-pg: DELETED** (was the only real cost, ~¥8/day storage).
- **Kept, ~¥0/mo**: corpus PDF bucket `my-rag-docs-bucket-123` (162MB),
  Phase 1 datastore `astronomy-2e-datastore` + engine. Kept for live demo.
- Total spend to date: exceeded the ¥2000/mo alert (one-time accumulation:
  ingestion + captions + embeddings + RAGAS debugging), NOT a runaway
  resource. Nothing bills daily anymore beyond a fraction of a cent of PDF
  storage.

## How to demo (free) / rebuild Phase 2 (paid)

See `docs/demo-guide.md`. Free demo = `RETRIEVAL_BACKEND=mock` API + `npm
start` → shows the bbox highlight with GCS-streamed PDF. Rebuild Phase 2
live = fresh Cloud SQL + `schema.sql` + `pipeline --split-astro-parts`
(caches make LLM ~free) + `RETRIEVAL_BACKEND=pgvector`; **test port 3307
first** (blocked on some networks).

## If resuming Phase 2 to completion

1. Finish the RAGAS A/B: rebuild Cloud SQL, run `run_eval.py --ragas` for
   Phase 2 (and Phase 1 via discovery_engine), fill the `‹RAGAS›` cells +
   5 case studies in `docs/phase2-ab-report.md`.
2. Local ingestion caches (`api/.ingest_cache/`, gitignored, 52MB) make
   re-ingestion ~free — don't delete them if a rebuild is likely.
3. `gemini-embedding-001` is the embedding model (Embedding 2 allowlist-
   gated). `.env` is set to `RETRIEVAL_BACKEND=mock` for free local dev.
